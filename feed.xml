<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="/feed.xml" rel="self" type="application/atom+xml" /><link href="/" rel="alternate" type="text/html" /><updated>2026-02-27T16:02:36+00:00</updated><id>/feed.xml</id><title type="html">Gapry’s Blog</title><subtitle></subtitle><entry><title type="html">Study Notes: Multiplying with a constant, Advent of Compiler Optimisations 2025</title><link href="/2026/02/27/Advent-of-Compiler-Optimisations-Study-Notes-04.html" rel="alternate" type="text/html" title="Study Notes: Multiplying with a constant, Advent of Compiler Optimisations 2025" /><published>2026-02-27T00:00:00+00:00</published><updated>2026-02-27T00:00:00+00:00</updated><id>/2026/02/27/Advent-of-Compiler-Optimisations-Study-Notes-04</id><content type="html" xml:base="/2026/02/27/Advent-of-Compiler-Optimisations-Study-Notes-04.html"><![CDATA[<h2 id="study-notes-multiplying-with-a-constant-advent-of-compiler-optimisations-2025">Study Notes: Multiplying with a constant, Advent of Compiler Optimisations 2025</h2>

<p>These notes are based on the post <a href="https://xania.org/202512/04-multiplying-integers"><strong>Multiplying with a constant</strong></a> and the YouTube video <a href="https://www.youtube.com/watch?v=1X88od0miHs&amp;list=PL2HVqYf7If8cY4wLk7JUQ2f0JXY_xMQm2&amp;index=5"><strong>[AoCO 4/25] Multiplying with a Constant</strong></a> which are Day 4 of the <a href="https://xania.org/AoCO2025-archive">Advent of Compiler Optimisations 2025</a> Series by <a href="https://xania.org/MattGodbolt">Matt Godbolt</a>.</p>

<p>My notes focus on reproducing and verifying <a href="https://xania.org/MattGodbolt">Matt Godbolt</a>’s teaching within a local development environment using <code class="language-plaintext highlighter-rouge">LLVM</code> toolchain on <code class="language-plaintext highlighter-rouge">Ubuntu</code>.</p>

<p>Selected technical insights from the YouTube comment section are reproduced at the end of these notes to provide additional context.</p>

<p>Written by me and assisted by AI, proofread by me and assisted by AI.</p>

<h4 id="development-environment">Development Environment</h4>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$ lsb_release -d
Description:	Ubuntu 24.04.3 LTS

$ clang -v
Ubuntu clang version 18.1.8

$ llvm-objdump -v
Ubuntu LLVM version 18.1.8

$ nvim --version
NVIM v0.11.5

$ echo $SHELL
/usr/bin/fish
</code></pre></div></div>

<h2 id="introduction">Introduction</h2>

<p>After studying the Day 04 YouTube video and blog post, I conducted a series of sequential tests by multiplying <code class="language-plaintext highlighter-rouge">x</code> by every constant from <code class="language-plaintext highlighter-rouge">2</code> to <code class="language-plaintext highlighter-rouge">20</code>.</p>

<p>By analyzing the assembly output for each case, I identified and selected 10 distinct compiler optimization strategies.</p>

<p>These notes document specific patterns and filter out redundant results, highlighting unique compiler optimization strategies.</p>

<h2 id="case-01--x--2">Case 01 : <code class="language-plaintext highlighter-rouge">x * 2</code></h2>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$ nvim mul.c
</code></pre></div></div>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>int mul(int x) {
  return x * 2;
}
</code></pre></div></div>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$ rm -f (path filter *.o); clang -O2 -c mul.c; llvm-objdump -d --x86-asm-syntax=att mul.o
</code></pre></div></div>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>mul.o:  file format elf64-x86-64

Disassembly of section .text:

0000000000000000 &lt;mul&gt;:
       0: 8d 04 3f                      leal    (%rdi,%rdi), %eax
       3: c3                            retq
</code></pre></div></div>

<p>The compiler avoids the <code class="language-plaintext highlighter-rouge">imul</code> instruction in favor of the <code class="language-plaintext highlighter-rouge">shift</code>, <code class="language-plaintext highlighter-rouge">add</code> and <code class="language-plaintext highlighter-rouge">lea</code> instructions for the following reasons:</p>

<ul>
  <li>Constant multiplication can often be expressed using free address generation</li>
  <li><code class="language-plaintext highlighter-rouge">lea</code> can compute <code class="language-plaintext highlighter-rouge">x + x * scale</code> without using ALU ports</li>
  <li>It may reduce dependency chains</li>
</ul>

<p>Here, it uses the leal instruction to perform <code class="language-plaintext highlighter-rouge">x + x</code> in a single cycle.</p>

<h2 id="case-02-x--3">Case 02: <code class="language-plaintext highlighter-rouge">x * 3</code></h2>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$ nvim mul.c
</code></pre></div></div>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>int mul(int x) {
  return x * 3;
}
</code></pre></div></div>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$ rm -f (path filter *.o); clang -O2 -c mul.c; llvm-objdump -d --x86-asm-syntax=att mul.o
</code></pre></div></div>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>mul.o:  file format elf64-x86-64

Disassembly of section .text:

0000000000000000 &lt;mul&gt;:
       0: 8d 04 7f                      leal    (%rdi,%rdi,2), %eax
       3: c3                            retq
</code></pre></div></div>

<p>It utilizes x86 the <code class="language-plaintext highlighter-rouge">lea</code> with <strong>Base + (Index * Scale)</strong> addressing to calculate <code class="language-plaintext highlighter-rouge">x + x * 2</code></p>

<h2 id="case-03-x--4">Case 03: <code class="language-plaintext highlighter-rouge">x * 4</code></h2>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$ nvim mul.c
</code></pre></div></div>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>int mul(int x) {
  return x * 4;
}
</code></pre></div></div>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$ rm -f (path filter *.o); clang -O2 -c mul.c; llvm-objdump -d --x86-asm-syntax=att mul.o
</code></pre></div></div>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>mul.o:  file format elf64-x86-64

Disassembly of section .text:

0000000000000000 &lt;mul&gt;:
       0: 8d 04 bd 00 00 00 00          leal    (,%rdi,4), %eax
       7: c3                            retq
</code></pre></div></div>

<p>Using the <code class="language-plaintext highlighter-rouge">lea</code> instruction with a scale factor enables multiplication to be performed in a single instruction, 
eliminating the need for separate shift and multiply instructions.</p>

<h2 id="case-04--x--6">Case 04 : <code class="language-plaintext highlighter-rouge">x * 6</code></h2>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$ nvim mul.c
</code></pre></div></div>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>int mul(int x) {
  return x * 6;
}
</code></pre></div></div>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$ rm -f (path filter *.o); clang -O2 -c mul.c; llvm-objdump -d --x86-asm-syntax=att mul.o
</code></pre></div></div>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>mul.o:  file format elf64-x86-64

Disassembly of section .text:

0000000000000000 &lt;mul&gt;:
       0: 01 ff                         addl    %edi, %edi
       2: 8d 04 7f                      leal    (%rdi,%rdi,2), %eax
       5: c3                            retq
</code></pre></div></div>

<p>The compiler splits the multiplication <code class="language-plaintext highlighter-rouge">x * 6</code> into two distinct steps: first <code class="language-plaintext highlighter-rouge">x + x</code>, followed by <code class="language-plaintext highlighter-rouge">2x + (2x * 2)</code>.</p>

<p>You might wonder why the compiler doesn’t simply use a single instruction like <code class="language-plaintext highlighter-rouge">leal (%rdi,%rdi,6), %eax</code>.</p>

<p>The reason lies in the instruction encoding.</p>

<p>The <code class="language-plaintext highlighter-rouge">leal</code> instruction calculates addresses using the formula <strong>Base + (Index * Scale)</strong>, where the scale factor is encoded at the bit level.</p>

<p>If we assume the scale field uses <strong>2 bits</strong> within the instruction encoding, there are only four possible binary combinations: <code class="language-plaintext highlighter-rouge">00</code>, <code class="language-plaintext highlighter-rouge">01</code>, <code class="language-plaintext highlighter-rouge">10</code>, and <code class="language-plaintext highlighter-rouge">11</code>.</p>

<p>These bits correspond to the multipliers <strong>1, 2, 4, and 8</strong>. Because of this limitation, it is not possible for the hardware to represent a scale of <code class="language-plaintext highlighter-rouge">6</code>.</p>

<p>Consequently, the compiler must break the operation into multiple valid steps to achieve the desired result.</p>

<h2 id="case-05--x--7">Case 05 : <code class="language-plaintext highlighter-rouge">x * 7</code></h2>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$ nvim mul.c
</code></pre></div></div>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>int mul(int x) {
  return x * 7;
}
</code></pre></div></div>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$ rm -f (path filter *.o); clang -O2 -c mul.c; llvm-objdump -d --x86-asm-syntax=att mul.o
</code></pre></div></div>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>mul.o:  file format elf64-x86-64

Disassembly of section .text:

0000000000000000 &lt;mul&gt;:
       0: 8d 04 fd 00 00 00 00          leal    (,%rdi,8), %eax
       7: 29 f8                         subl    %edi, %eax
       9: c3                            retq
</code></pre></div></div>

<p>The compiler uses <strong>multiply and subtract</strong> logic: It first scales <code class="language-plaintext highlighter-rouge">x</code> by 8 using leal and then subtracts the original <code class="language-plaintext highlighter-rouge">x</code>.</p>

<h2 id="case-06--x--11">Case 06 : <code class="language-plaintext highlighter-rouge">x * 11</code></h2>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$ nvim mul.c
</code></pre></div></div>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>int mul(int x) {
  return x * 11;
}
</code></pre></div></div>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$ rm -f (path filter *.o); clang -O2 -c mul.c; llvm-objdump -d --x86-asm-syntax=att mul.o
</code></pre></div></div>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>mul.o:  file format elf64-x86-64

Disassembly of section .text:

0000000000000000 &lt;mul&gt;:
       0: 8d 04 bf                      leal    (%rdi,%rdi,4), %eax
       3: 8d 04 47                      leal    (%rdi,%rax,2), %eax
       6: c3                            retq
</code></pre></div></div>

<p>The compiler splits the multiplication <code class="language-plaintext highlighter-rouge">x * 11</code> into two distinct steps: 
first <code class="language-plaintext highlighter-rouge">x + x * 4</code>, followed by <code class="language-plaintext highlighter-rouge">x + (5x * 2)</code>.</p>

<h2 id="case-07--x--12">Case 07 : <code class="language-plaintext highlighter-rouge">x * 12</code></h2>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$ nvim mul.c
</code></pre></div></div>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>int mul(int x) {
  return x * 12;
}
</code></pre></div></div>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$ rm -f (path filter *.o); clang -O2 -c mul.c; llvm-objdump -d --x86-asm-syntax=att mul.o
</code></pre></div></div>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>mul.o:  file format elf64-x86-64

Disassembly of section .text:

0000000000000000 &lt;mul&gt;:
       0: c1 e7 02                      shll    $0x2, %edi
       3: 8d 04 7f                      leal    (%rdi,%rdi,2), %eax
       6: c3                            retq
</code></pre></div></div>

<p>The compiler splits the multiplication <code class="language-plaintext highlighter-rouge">x * 12</code> into two distinct steps: 
first <code class="language-plaintext highlighter-rouge">x &lt;&lt; 2</code>, followed by <code class="language-plaintext highlighter-rouge">4x + (4x * 2)</code>.</p>

<h2 id="case-08-x--14">Case 08: <code class="language-plaintext highlighter-rouge">x * 14</code></h2>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$ nvim mul.c
</code></pre></div></div>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>int mul(int x) {
  return x * 14;
}
</code></pre></div></div>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$ rm -f (path filter *.o); clang -O2 -c mul.c; llvm-objdump -d --x86-asm-syntax=att mul.o
</code></pre></div></div>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>mul.o:  file format elf64-x86-64

Disassembly of section .text:

0000000000000000 &lt;mul&gt;:
       0: 89 f8                         movl    %edi, %eax
       2: 8d 0c 00                      leal    (%rax,%rax), %ecx
       5: c1 e0 04                      shll    $0x4, %eax
       8: 29 c8                         subl    %ecx, %eax
       a: c3                            retq
</code></pre></div></div>

<p>The compiler splits the multiplication <code class="language-plaintext highlighter-rouge">x * 14</code> into three distinct steps:</p>
<ol>
  <li>calculates 2x and store it in ecx (<code class="language-plaintext highlighter-rouge">ecx = x + x</code>)</li>
  <li>calculates 16x and store it in eax (<code class="language-plaintext highlighter-rouge">eax = 16 * x</code>)</li>
  <li>calculates 14x by subtracting the two results (<code class="language-plaintext highlighter-rouge">eax - ecx = 16x - 2x = 14x</code>)</li>
</ol>

<h2 id="case-09-x--16">Case 09: <code class="language-plaintext highlighter-rouge">x * 16</code></h2>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$ nvim mul.c
</code></pre></div></div>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>int mul(int x) {
  return x * 16;
}
</code></pre></div></div>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$ rm -f (path filter *.o); clang -O2 -c mul.c; llvm-objdump -d --x86-asm-syntax=att mul.o
</code></pre></div></div>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>mul.o:  file format elf64-x86-64

Disassembly of section .text:

0000000000000000 &lt;mul&gt;:
       0: 89 f8                         movl    %edi, %eax
       2: c1 e0 04                      shll    $0x4, %eax
       5: c3                            retq
</code></pre></div></div>

<p>According the x86-64 calling convention, the result must be returned in %eax. 
The compiler cannot simply generate a single <code class="language-plaintext highlighter-rouge">shll $0x4, %edi</code> instruction.
It need to generate an extra instruction to move the input value from <code class="language-plaintext highlighter-rouge">%edi</code> to <code class="language-plaintext highlighter-rouge">%eax</code> before the shift operation.</p>

<h2 id="case-10-x--17">Case 10: <code class="language-plaintext highlighter-rouge">x * 17</code></h2>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$ nvim mul.c
</code></pre></div></div>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>int mul(int x) {
  return x * 17;
}
</code></pre></div></div>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$ rm -f (path filter *.o); clang -O2 -c mul.c; llvm-objdump -d --x86-asm-syntax=att mul.o
</code></pre></div></div>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>mul.o:  file format elf64-x86-64

Disassembly of section .text:

0000000000000000 &lt;mul&gt;:
       0: 89 f8                         movl    %edi, %eax
       2: c1 e0 04                      shll    $0x4, %eax
       5: 01 f8                         addl    %edi, %eax
       7: c3                            retq
</code></pre></div></div>

<p>Similar to case 09, we first need to move the input value from <code class="language-plaintext highlighter-rouge">%edi</code> to <code class="language-plaintext highlighter-rouge">%eax</code> to satisfy the calling convention. 
Then we get <code class="language-plaintext highlighter-rouge">16 * x</code>. Finally, we get <code class="language-plaintext highlighter-rouge">x + 16x</code>.</p>

<h2 id="youtube-comment-insights">YouTube Comment Insights</h2>

<p>Since YouTube does not currently support generating direct permanent links to individual comments, 
I have reproduced the relevant technical insight below in its entirety to ensure both accuracy and proper attribution.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>@moregirl4585
Fact: on some architecture x*-3 is better expressed as x-(x&lt;&lt;2) and some better as -(x+x+x). Seems compilers don't work well for both case

@SLiV9
Do you know why for multiplying by 6, it uses an ADD for the second x2? Why not another LEA like for multiplying by 2?
|
|--&gt; @nurmr
|    My suspicion is that address adder is "cheaper" to use than an ALU. 
|    Especially with more complex code which might have other operations pipelined and running on an ALU. 
|
|--&gt; @HenryLoenwind 
     lea is faster than mov+add, but not faster than a "naked" add. 
     And even if the cpu cycles are the same in the end, 
     add reg,reg is a 2-byte instruction (like the xor we had on day 1).

@lpprogrammingllc 
It's worth noting the shifts-and-adds version might still be faster on modern CPUs due to instruction pipelining. 
Yes, the total work done is more than a single imul, but it can do each part in parallel, until the final add. 
However, it also requires more code, and more "slots" in the decoding/execution pipeline. 
Lots of modern machines have a 4-wide instruction frontend. 
So with the imul, one of them handles the imul, and 3 keep going on any other calculations they can. 
With the shifts-and-adds version, 3 get held up waiting on the shifts, to then issue the adds. 
Only one gets to work on anything else.  
The compiler assumes there will usually be other work to do, 
so aims for maximum throughput rather than prioritizing finishing the mult as soon as possible.

@lpprogrammingllc
The CPU execution backend may well execute shifts and adds. However between your code in memory and that backend is the instruction decoder, 
which uses the microcode to turn your single imul into whatever real micro instructions are required for the execution backend.

If I understand the implicit part of your comment correctly, 
you think there is no value in giving the instruction decoder the split-apart shift-and-add instructions because what the backend executes may be the same in either case. 
This is incorrect. The cyclic latency (the number of cycles from beginning decode to commit) in the imul instruction is the number of cycles required for the instruction decoder 
to issue microcode to the backend and for the backend to execute that microcode. Often the limiting factor is the frontend decode speed. 
If you can decrease the number of cycles spent by the frontend doing the decode, you can decrease the total cycles required, 
at the expense of more power use or less other work happening at the same time.
</code></pre></div></div>

<h2 id="references">References</h2>
<p>[1] https://cs61.seas.harvard.edu/site/2018/Asm1/</p>]]></content><author><name></name></author><category term="compiler" /><summary type="html"><![CDATA[Study Notes: Multiplying with a constant, Advent of Compiler Optimisations 2025]]></summary></entry><entry><title type="html">Study Notes: Addressing the adding situation</title><link href="/2026/01/31/Advent-of-Compiler-Optimisations-Study-Notes-02.html" rel="alternate" type="text/html" title="Study Notes: Addressing the adding situation" /><published>2026-01-31T00:00:00+00:00</published><updated>2026-01-31T00:00:00+00:00</updated><id>/2026/01/31/Advent-of-Compiler-Optimisations-Study-Notes-02</id><content type="html" xml:base="/2026/01/31/Advent-of-Compiler-Optimisations-Study-Notes-02.html"><![CDATA[<h2 id="study-notes-addressing-the-adding-situation">Study Notes: Addressing the adding situation</h2>

<p>These notes are based on the post <a href="https://xania.org/202512/02-adding-integers"><strong>Addressing the adding situation</strong></a> and the YouTube video <a href="https://www.youtube.com/watch?v=BOvg0sGJnes&amp;list=PL2HVqYf7If8cY4wLk7JUQ2f0JXY_xMQm2&amp;index=3"><strong>[AoCO 2/25] Adding Integers on x86 - just an ADD, right?</strong></a> which are Day 2 of the <a href="https://xania.org/AoCO2025-archive">Advent of Compiler Optimisations 2025</a> Series by <a href="https://xania.org/MattGodbolt">Matt Godbolt</a>.</p>

<p>My notes focus on reproducing and verifying <a href="https://xania.org/MattGodbolt">Matt Godbolt</a>’s teaching within a local development environment using <code class="language-plaintext highlighter-rouge">LLVM</code> toolchain on <code class="language-plaintext highlighter-rouge">Ubuntu</code>.</p>

<p>Additionally, I have extended the discussion by implementing a manual Proof of Concept in assembly to demonstrate the equivalence of <code class="language-plaintext highlighter-rouge">add</code> vs <code class="language-plaintext highlighter-rouge">lea</code> instructions.</p>

<p>Selected technical insights from the YouTube comment section are reproduced at the end of these notes to provide additional context.</p>

<p>Written by me and assisted by AI, proofread by me and assisted by AI.</p>

<h4 id="development-environment">Development Environment</h4>

<figure class="highlight"><pre><code class="language-bash" data-lang="bash"><span class="nv">$ </span>lsb_release <span class="nt">-d</span>
Description:	Ubuntu 24.04.3 LTS

<span class="nv">$ </span>clang <span class="nt">-v</span>
Ubuntu clang version 18.1.8

<span class="nv">$ </span>llvm-objdump <span class="nt">-v</span>
Ubuntu LLVM version 18.1.8

<span class="nv">$ </span><span class="nb">echo</span> <span class="nv">$SHELL</span>
/usr/bin/fish</code></pre></figure>

<h2 id="integer-addition">Integer Addition</h2>

<p>To understand how <code class="language-plaintext highlighter-rouge">clang</code> translates <code class="language-plaintext highlighter-rouge">C</code> addition into <code class="language-plaintext highlighter-rouge">x86-64</code> machine instructions, we use the following implementation</p>

<figure class="highlight"><pre><code class="language-bash" data-lang="bash"><span class="nv">$ </span>nvim add.c</code></pre></figure>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">int</span> <span class="nf">add</span><span class="p">(</span><span class="kt">int</span> <span class="n">x</span><span class="p">,</span> <span class="kt">int</span> <span class="n">y</span><span class="p">)</span> <span class="p">{</span>
  <span class="k">return</span> <span class="n">x</span> <span class="o">+</span> <span class="n">y</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div>

<h2 id="unoptimized-analysis">Unoptimized Analysis</h2>

<figure class="highlight"><pre><code class="language-bash" data-lang="bash"><span class="nv">$ </span><span class="nb">rm</span> <span class="nt">-f</span> <span class="o">(</span>path filter <span class="k">*</span>.o<span class="o">)</span><span class="p">;</span> clang <span class="nt">-O0</span> <span class="nt">-c</span> add.c<span class="p">;</span> llvm-objdump <span class="nt">-d</span> <span class="nt">--x86-asm-syntax</span><span class="o">=</span>att add.o</code></pre></figure>

<figure class="highlight"><pre><code class="language-bash" data-lang="bash">add.o:  file format elf64-x86-64

Disassembly of section .text:

0000000000000000 &lt;add&gt;:
       0: 55                            pushq   %rbp
       1: 48 89 e5                      movq    %rsp, %rbp
       4: 89 7d <span class="nb">fc                      </span>movl    %edi, <span class="nt">-0x4</span><span class="o">(</span>%rbp<span class="o">)</span>
       7: 89 75 f8                      movl    %esi, <span class="nt">-0x8</span><span class="o">(</span>%rbp<span class="o">)</span>
       a: 8b 45 <span class="nb">fc                      </span>movl    <span class="nt">-0x4</span><span class="o">(</span>%rbp<span class="o">)</span>, %eax
       d: 03 45 f8                      addl    <span class="nt">-0x8</span><span class="o">(</span>%rbp<span class="o">)</span>, %eax
      10: 5d                            popq    %rbp
      11: c3                            retq</code></pre></figure>

<p>In C, the expression <code class="language-plaintext highlighter-rouge">a = b + c</code> allows for three distinct variables to execute the addition. 
However, the x86-64 ISA does not support a three-operand format for standard addition.
The format for the <code class="language-plaintext highlighter-rouge">add</code> instruction is <code class="language-plaintext highlighter-rouge">add source, destination</code>, which executes the operation
<code class="language-plaintext highlighter-rouge">destination = destination + source</code>.</p>

<p>Because the hardware logic requires the destination register to overlap with one of the source operands,
the compiler cannot translate <code class="language-plaintext highlighter-rouge">a = b + c</code> directly to a single <code class="language-plaintext highlighter-rouge">add</code> instruction. To prevent overwriting the original 
value of <code class="language-plaintext highlighter-rouge">b</code> or <code class="language-plaintext highlighter-rouge">c</code> before the operation is executed, the compiler needs to use <code class="language-plaintext highlighter-rouge">mov</code> instruction to 
initialize the destination with one of the operands first:</p>

<figure class="highlight"><pre><code class="language-gas" data-lang="gas">movl    -0x4(%rbp), %eax
addl    -0x8(%rbp), %eax</code></pre></figure>

<p>Hence, the compiler needs to use two instructions to execute the addition at the -O0 level.</p>

<h2 id="optimized-analysis">Optimized Analysis</h2>

<figure class="highlight"><pre><code class="language-bash" data-lang="bash"><span class="nb">rm</span> <span class="nt">-f</span> <span class="o">(</span>path filter <span class="k">*</span>.o<span class="o">)</span><span class="p">;</span> clang <span class="nt">-O2</span> <span class="nt">-c</span> add.c<span class="p">;</span> llvm-objdump <span class="nt">-d</span> <span class="nt">--x86-asm-syntax</span><span class="o">=</span>att add.o</code></pre></figure>

<figure class="highlight"><pre><code class="language-bash" data-lang="bash">add.o:  file format elf64-x86-64

Disassembly of section .text:

0000000000000000 &lt;add&gt;:
       0: 8d 04 37                      leal    <span class="o">(</span>%rdi,%rsi<span class="o">)</span>, %eax
       3: c3                            retq</code></pre></figure>

<p>At the <code class="language-plaintext highlighter-rouge">-O2</code> level, the compiler translates the <code class="language-plaintext highlighter-rouge">C</code> statement <code class="language-plaintext highlighter-rouge">return x + y;</code> directly into a single <code class="language-plaintext highlighter-rouge">lea</code> instruction. 
Because lea supports two source registers, the compiler can take two independent inputs (<code class="language-plaintext highlighter-rouge">%rdi</code> and <code class="language-plaintext highlighter-rouge">%rsi</code>) and 
store the result in an independent destination (<code class="language-plaintext highlighter-rouge">%eax</code>) without overwriting the original operands.
This allows the <code class="language-plaintext highlighter-rouge">a = b + c</code> logic to be executed in one step, 
eliminating the need for the extra mov instruction required at the <code class="language-plaintext highlighter-rouge">-O0</code> level.</p>

<h2 id="proof-of-concept">Proof of Concept</h2>
<p>The following assembly code demonstrates these two approaches: 
one utilizing the <code class="language-plaintext highlighter-rouge">mov</code> + <code class="language-plaintext highlighter-rouge">add</code> instruction sequence, 
and the other employing a single <code class="language-plaintext highlighter-rouge">lea</code> instruction.</p>

<figure class="highlight"><pre><code class="language-bash" data-lang="bash"><span class="nv">$ </span>nvim add.s</code></pre></figure>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>.section .note.GNU-stack, "", @progbits

.section .rodata
  fmt: .string "Result: %d\n"

.section .text
  .globl main
  .extern printf

main:
  pushq   %rbp
  movq    %rsp, %rbp

  # --- Case A: Using mov + add ---
  movl    $1, %edx
  movl    $2, %ecx
  movl    %edx, %eax
  addl    %ecx, %eax

  # Print Result
  movq    fmt@GOTPCREL(%rip), %rdi
  movslq  %eax, %rsi
  movl    $0, %eax
  call    printf

  # --- Case B: Using lea ---
  movl    $1, %edx
  movl    $2, %ecx
  leal    (%edx, %ecx), %eax

  # Print Result
  movq    fmt@GOTPCREL(%rip), %rdi
  movslq  %eax, %rsi
  movl    $0, %eax
  call    printf

  movl    $0, %eax
  popq    %rbp
  retq
</code></pre></div></div>

<figure class="highlight"><pre><code class="language-bash" data-lang="bash"><span class="nv">$ </span><span class="nb">rm</span> <span class="nt">-f</span> <span class="o">(</span>path filter <span class="k">*</span>.out<span class="o">)</span><span class="p">;</span> clang <span class="nt">-o</span> add.out add.s<span class="p">;</span> ./add.out
Result: 3
Result: 3</code></pre></figure>

<p>As demonstrated, both approaches produce identical results, confirming that the single <code class="language-plaintext highlighter-rouge">lea</code> instruction is 
logically equivalent to the <code class="language-plaintext highlighter-rouge">a = b + c</code> mathematical operation.</p>

<h2 id="youtube-comment-insights">YouTube Comment Insights</h2>

<p>Since YouTube does not currently support generating direct permanent links to individual comments, 
I have reproduced the relevant technical insight below in its entirety to ensure both accuracy and proper attribution.</p>

<figure class="highlight"><pre><code class="language-text" data-lang="text">@sulix314
LEA doesn't affect flags. While this is sometimes annoying (when you need to carry with ADC), 
it is often extremely useful because you can perform arithmetic without destroying the flag state 
needed for a subsequent conditional jump or another calculation.

@incubus3827
In addition, LEA could run on the V-pipeline in the original Pentium, which often allowed performing some arithmetics + reshuffling registers 
for no additional cycles. A true gamechanger for software rasterizers.

@mytech6779 
I recall the LEA instruction also uses a dedicated module on the CPU with an independent execution pipeline, 
so the LEA operation can be concurrent with an ALU operation. 
I can't say the address module addition [in isolation] is faster or the same cycle count as the ALU, 
but being specialized I imagine the address module is somewhat simpler with fewer transistors 
(reducing area and heat some small amount).</code></pre></figure>]]></content><author><name></name></author><category term="compiler" /><summary type="html"><![CDATA[Study Notes: Addressing the adding situation]]></summary></entry><entry><title type="html">Study Notes: You can’t fool the optimiser</title><link href="/2026/01/31/Advent-of-Compiler-Optimisations-Study-Notes-03.html" rel="alternate" type="text/html" title="Study Notes: You can’t fool the optimiser" /><published>2026-01-31T00:00:00+00:00</published><updated>2026-01-31T00:00:00+00:00</updated><id>/2026/01/31/Advent-of-Compiler-Optimisations-Study-Notes-03</id><content type="html" xml:base="/2026/01/31/Advent-of-Compiler-Optimisations-Study-Notes-03.html"><![CDATA[<h2 id="study-notes-you-cant-fool-the-optimiser">Study Notes: You can’t fool the optimiser</h2>

<p>These notes are based on the post <a href="https://xania.org/202512/03-more-adding-integers"><strong>You can’t fool the optimiser</strong></a> and the YouTube video <a href="https://www.youtube.com/watch?v=wHg9lYPMvvE&amp;list=PL2HVqYf7If8cY4wLk7JUQ2f0JXY_xMQm2&amp;index=4"><strong>[AoCO 3/25] More Adding</strong></a> which are Day 3 of the <a href="https://xania.org/AoCO2025-archive">Advent of Compiler Optimisations 2025</a> Series by <a href="https://xania.org/MattGodbolt">Matt Godbolt</a>.</p>

<p>My notes focus on reproducing and verifying <a href="https://xania.org/MattGodbolt">Matt Godbolt</a>’s teaching within a local development environment using <code class="language-plaintext highlighter-rouge">LLVM</code> toolchain on <code class="language-plaintext highlighter-rouge">Ubuntu</code>.</p>

<p>Written by me and assisted by AI, proofread by me and assisted by AI.</p>

<h2 id="development-environment">Development Environment</h2>

<figure class="highlight"><pre><code class="language-bash" data-lang="bash"><span class="nv">$ </span>lsb_release <span class="nt">-d</span>
Description:	Ubuntu 24.04.3 LTS

<span class="nv">$ </span>clang <span class="nt">-v</span>
Ubuntu clang version 18.1.8

<span class="nv">$ </span><span class="nb">sudo </span>apt <span class="nb">install </span>gcc-aarch64-linux-gnu libc6-dev-arm64-cross
<span class="nv">$ </span>aarch64-linux-gnu-gcc <span class="nt">-v</span>
<span class="nv">COLLECT_GCC</span><span class="o">=</span>aarch64-linux-gnu-gcc
gcc version 13.3.0 <span class="o">(</span>Ubuntu 13.3.0-6ubuntu2~24.04<span class="o">)</span>

<span class="nv">$ </span>qemu-aarch64 <span class="nt">-version</span>
qemu-aarch64 version 8.2.2 <span class="o">(</span>Debian 1:8.2.2+ds-0ubuntu1.11<span class="o">)</span>

<span class="nv">$ </span>llvm-objdump <span class="nt">-v</span>
Ubuntu LLVM version 18.1.8

<span class="nv">$ </span><span class="nb">echo</span> <span class="nv">$SHELL</span>
/usr/bin/fish</code></pre></figure>

<h2 id="recursion-integer-addition">Recursion Integer Addition</h2>

<p>We can observe the compiler’s optimization by implementing addition through recursion, as shown in the following code:</p>

<figure class="highlight"><pre><code class="language-bash" data-lang="bash"><span class="nv">$ </span>nvim add.c</code></pre></figure>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#include</span> <span class="cpf">&lt;stdio.h&gt;</span><span class="cp">
</span>
<span class="kt">unsigned</span> <span class="nf">add</span><span class="p">(</span><span class="kt">unsigned</span> <span class="n">x</span><span class="p">,</span> <span class="kt">unsigned</span> <span class="n">y</span><span class="p">)</span> <span class="p">{</span>
  <span class="k">return</span> <span class="n">y</span> <span class="o">&lt;=</span> <span class="mi">0</span> <span class="o">?</span> <span class="n">x</span> <span class="o">:</span> <span class="n">add</span><span class="p">(</span><span class="n">x</span> <span class="o">+</span> <span class="mi">1</span><span class="p">,</span> <span class="n">y</span> <span class="o">-</span> <span class="mi">1</span><span class="p">);</span>
<span class="p">}</span>

<span class="kt">int</span> <span class="nf">main</span><span class="p">(</span><span class="kt">void</span><span class="p">)</span> <span class="p">{</span>
  <span class="kt">unsigned</span> <span class="n">a</span> <span class="o">=</span> <span class="mi">1</span><span class="p">;</span>
  <span class="kt">unsigned</span> <span class="n">b</span> <span class="o">=</span> <span class="mi">10</span><span class="p">;</span>
  <span class="kt">unsigned</span> <span class="n">r</span> <span class="o">=</span> <span class="n">add</span><span class="p">(</span><span class="n">a</span><span class="p">,</span> <span class="n">b</span><span class="p">);</span>
  <span class="n">printf</span><span class="p">(</span><span class="s">"%u = %u</span><span class="se">\n</span><span class="s">"</span><span class="p">,</span> <span class="n">r</span><span class="p">,</span> <span class="n">a</span> <span class="o">+</span> <span class="n">b</span><span class="p">);</span>
  <span class="k">return</span> <span class="mi">0</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div>

<h2 id="unoptimized-analysis">Unoptimized Analysis</h2>

<p>The following analysis examines the unoptimized assembly code generated with the <code class="language-plaintext highlighter-rouge">-O0</code> flag.</p>

<figure class="highlight"><pre><code class="language-bash" data-lang="bash"><span class="nv">$ </span><span class="nb">rm</span> <span class="nt">-f</span> <span class="o">(</span>path filter <span class="k">*</span>.out<span class="o">)</span><span class="p">;</span> clang <span class="nt">-O0</span> <span class="nt">-target</span> aarch64-linux-gnu <span class="nt">--sysroot</span><span class="o">=</span>/usr/aarch64-linux-gnu <span class="nt">-static</span> add.c <span class="nt">-o</span> app.out<span class="p">;</span> qemu-aarch64 ./app.out
11 <span class="o">=</span> 11</code></pre></figure>

<figure class="highlight"><pre><code class="language-bash" data-lang="bash"><span class="nv">$ </span>llvm-objdump <span class="nt">-d</span> <span class="nt">--disassemble-symbols</span><span class="o">=</span>add app.out

app.out:        file format elf64-littleaarch64

Disassembly of section .text:

00000000004007f0 &lt;add&gt;:
  4007f0: d10083ff      sub     sp, sp, <span class="c">#0x20           // Allocate 32 bytes on stack</span>
  4007f4: a9017bfd      stp     x29, x30, <span class="o">[</span>sp, <span class="c">#0x10]   // Save Frame Pointer (x29) and Link Register (x30)</span>
  4007f8: 910043fd      add     x29, sp, <span class="c">#0x10          // Set up new Frame Pointer</span>
  4007fc: b81fc3a0      stur    w0, <span class="o">[</span>x29, <span class="c">#-0x4]        // Store 'x' (w0) into stack</span>
  400800: b9000be1      str     w1, <span class="o">[</span>sp, <span class="c">#0x8]          // Store 'y' (w1) into stack</span>
  400804: b9400be8      ldr     w8, <span class="o">[</span>sp, <span class="c">#0x8]          // Load 'y' from stack into w8</span>
  400808: 71000108      subs    w8, w8, <span class="c">#0x0            // Compare w8 (y) with 0</span>
  40080c: 540000a8      b.hi    0x400820 &lt;add+0x30&gt;     // If y <span class="o">&gt;</span> 0, jump to recursive <span class="k">case</span> <span class="o">(</span>400820<span class="p">)</span>
  400810: 14000001      b       0x400814 &lt;add+0x24&gt;     // Else, branch to base <span class="k">case</span> logic
  400814: b85fc3a0      ldur    w0, <span class="o">[</span>x29, <span class="c">#-0x4]        // [Base Case] Load 'x' into w0</span>
  400818: b90007e0      str     w0, <span class="o">[</span>sp, <span class="c">#0x4]          // Store 'x' as the potential return value</span>
  40081c: 14000008      b       0x40083c &lt;add+0x4c&gt;     // Jump to epilogue <span class="o">(</span><span class="k">return</span><span class="p">)</span>
  400820: b85fc3a8      ldur    w8, <span class="o">[</span>x29, <span class="c">#-0x4]        // [Recursive Case] Load 'x' into w8</span>
  400824: 11000500      add     w0, w8, <span class="c">#0x1            // w0 = x + 1 (Preparing 1st argument)</span>
  400828: b9400be8      ldr     w8, <span class="o">[</span>sp, <span class="c">#0x8]          // Load 'y' into w8</span>
  40082c: 71000501      subs    w1, w8, <span class="c">#0x1            // w1 = y - 1 (Preparing 2nd argument)</span>
  400830: 97fffff0      bl      0x4007f0 &lt;add&gt;          // Recursive call: add<span class="o">(</span>x + 1, y - 1<span class="o">)</span>
  400834: b90007e0      str     w0, <span class="o">[</span>sp, <span class="c">#0x4]          // Store the recursive result onto stack</span>
  400838: 14000001      b       0x40083c &lt;add+0x4c&gt;     // Jump to epilogue <span class="o">(</span><span class="k">return</span><span class="o">)</span>
  40083c: b94007e0      ldr     w0, <span class="o">[</span>sp, <span class="c">#0x4]          // Load the result from stack into w0</span>
  400840: a9417bfd      ldp     x29, x30, <span class="o">[</span>sp, <span class="c">#0x10]   // Restore Frame Pointer and Link Register</span>
  400844: 910083ff      add     sp, sp, <span class="c">#0x20           // Deallocate stack space</span>
  400848: d65f03c0      ret                             // Return to <span class="nb">caller</span></code></pre></figure>

<h4 id="part-01-function-prologue">Part 01: Function Prologue</h4>

<figure class="highlight"><pre><code class="language-bash" data-lang="bash">4007f0: d10083ff      sub     sp, sp, <span class="c">#0x20           // Allocate 32 bytes on stack</span>
4007f4: a9017bfd      stp     x29, x30, <span class="o">[</span>sp, <span class="c">#0x10]   // Save Frame Pointer (x29) and Link Register (x30)</span>
4007f8: 910043fd      add     x29, sp, <span class="c">#0x10          // Set up new Frame Pointer</span></code></pre></figure>

<p>Each recursive invocation initiates a Function Prologue to establish the execution context. 
The instruction <code class="language-plaintext highlighter-rouge">sub sp, sp, #0x20</code> performs Stack Allocation by decrementing the Stack Pointer (<code class="language-plaintext highlighter-rouge">SP</code>), reserving 32 bytes for the current Stack Frame.
The subsequent <code class="language-plaintext highlighter-rouge">stp</code> (Store Pair) instruction implements Context Saving, pushing the Frame Pointer (<code class="language-plaintext highlighter-rouge">X29</code>) and Link Register (<code class="language-plaintext highlighter-rouge">X30</code>) onto the stack to 
facilitate the Stack Unwinding process during the Function Epilogue.</p>

<figure class="highlight"><pre><code class="language-bash" data-lang="bash">| Higher Address |
|                |
+----------------+ &lt;<span class="nt">---</span> Previous SP
| Unused / Local | <span class="o">(</span>16 bytes<span class="o">)</span>
+----------------+ 
|  Link Register | <span class="o">(</span>X30<span class="o">)</span>
+----------------+ &lt;<span class="nt">---</span> X29 <span class="o">(</span>New Frame Pointer<span class="o">)</span>
|  Frame Pointer | <span class="o">(</span>X29<span class="o">)</span>
+----------------+ &lt;<span class="nt">---</span> SP  <span class="o">(</span>Current Stack Pointer<span class="o">)</span>
|                |
|  Lower Address |  </code></pre></figure>

<h4 id="part-02-parameter-storage">Part 02: Parameter Storage</h4>

<figure class="highlight"><pre><code class="language-bash" data-lang="bash">4007fc: b81fc3a0      stur    w0, <span class="o">[</span>x29, <span class="c">#-0x4]        // Store 'x' (w0) into stack</span>
400800: b9000be1      str     w1, <span class="o">[</span>sp, <span class="c">#0x8]          // Store 'y' (w1) into stack</span>
400804: b9400be8      ldr     w8, <span class="o">[</span>sp, <span class="c">#0x8]          // Load 'y' from stack into w8</span></code></pre></figure>

<p>The input parameters <code class="language-plaintext highlighter-rouge">x</code> and <code class="language-plaintext highlighter-rouge">y</code> are stored from registers (<code class="language-plaintext highlighter-rouge">w0</code> and <code class="language-plaintext highlighter-rouge">w1</code>) into stack memory. 
Since it is at the <code class="language-plaintext highlighter-rouge">-O0</code> optimization level, 
an additional instruction is used to load <code class="language-plaintext highlighter-rouge">y</code> from stack memory back into a register (<code class="language-plaintext highlighter-rouge">w8</code>) for subsequent conditional evaluation.</p>

<h4 id="part-03-branching">Part 03: Branching</h4>

<figure class="highlight"><pre><code class="language-bash" data-lang="bash">400808: 71000108      subs    w8, w8, <span class="c">#0x0            // Compare w8 (y) with 0</span>
40080c: 540000a8      b.hi    0x400820 &lt;add+0x30&gt;     // If y <span class="o">&gt;</span> 0, jump to recursive <span class="k">case</span> <span class="o">(</span>400820<span class="p">)</span>
400810: 14000001      b       0x400814 &lt;add+0x24&gt;     // Else, branch to base <span class="k">case</span> logic</code></pre></figure>

<p>The subs instruction performs an arithmetic subtraction to compare <code class="language-plaintext highlighter-rouge">y</code> (in <code class="language-plaintext highlighter-rouge">w8</code>) with <code class="language-plaintext highlighter-rouge">0</code>, 
updating the Condition Flags in the processor’s state register. 
The <code class="language-plaintext highlighter-rouge">b.hi</code> (Branch if Higher) instruction then evaluates these flags: 
if <code class="language-plaintext highlighter-rouge">y &gt; 0</code>, the Program Counter (<code class="language-plaintext highlighter-rouge">PC</code>) jumps to the Recursive Case; 
otherwise, it jumps to the Base Case.</p>

<h4 id="part-04-the-base-case-y--0">Part 04: The Base Case: <code class="language-plaintext highlighter-rouge">y == 0</code></h4>

<figure class="highlight"><pre><code class="language-bash" data-lang="bash">400814: b85fc3a0      ldur    w0, <span class="o">[</span>x29, <span class="c">#-0x4]        // [Base Case] Load 'x' into w0</span>
400818: b90007e0      str     w0, <span class="o">[</span>sp, <span class="c">#0x4]          // Store 'x' as the potential return value</span>
40081c: 14000008      b       0x40083c &lt;add+0x4c&gt;     // Jump to epilogue <span class="o">(</span><span class="k">return</span><span class="o">)</span> <span class="o">(</span>Part 06<span class="o">)</span></code></pre></figure>

<p>When the base case is met, the value of <code class="language-plaintext highlighter-rouge">x</code> is loaded into register <code class="language-plaintext highlighter-rouge">W0</code>. 
The compiler then executes a <code class="language-plaintext highlighter-rouge">store</code> operation from register <code class="language-plaintext highlighter-rouge">W0</code> to stack memory to preserve the 
return value.</p>

<h4 id="part-05-the-recursive-step-addx--1-y---1">Part 05: The Recursive Step: <code class="language-plaintext highlighter-rouge">add(x + 1, y - 1)</code></h4>

<figure class="highlight"><pre><code class="language-bash" data-lang="bash">400820: b85fc3a8      ldur    w8, <span class="o">[</span>x29, <span class="c">#-0x4]        // [Recursive Case] Load 'x' into w8</span>
400824: 11000500      add     w0, w8, <span class="c">#0x1            // w0 = x + 1 (Preparing 1st argument)</span>
400828: b9400be8      ldr     w8, <span class="o">[</span>sp, <span class="c">#0x8]          // Load 'y' into w8</span>
40082c: 71000501      subs    w1, w8, <span class="c">#0x1            // w1 = y - 1 (Preparing 2nd argument)</span>
400830: 97fffff0      bl      0x4007f0 &lt;add&gt;          // Recursive call: add<span class="o">(</span>x + 1, y - 1<span class="o">)</span>
400834: b90007e0      str     w0, <span class="o">[</span>sp, <span class="c">#0x4]          // Store the recursive result onto stack</span>
400838: 14000001      b       0x40083c &lt;add+0x4c&gt;     // Jump to epilogue <span class="o">(</span><span class="k">return</span><span class="o">)</span></code></pre></figure>

<p>The compiler prepares the arguments for the recursive call by loading values from the stack into registers <code class="language-plaintext highlighter-rouge">w0</code> (<code class="language-plaintext highlighter-rouge">x + 1</code>) and <code class="language-plaintext highlighter-rouge">w1</code> (<code class="language-plaintext highlighter-rouge">y - 1</code>) according to the Procedure Call Standard. 
The <code class="language-plaintext highlighter-rouge">bl</code> (Branch with Link) instruction then executes the recursive call, redirecting the Control Flow back to the function start. 
Once the recursive call returns, the resulting value in <code class="language-plaintext highlighter-rouge">w0</code> is stored into stack memory before jumping to the epilogue.</p>

<h4 id="part-06-function-epilogue">Part 06: Function Epilogue</h4>

<figure class="highlight"><pre><code class="language-bash" data-lang="bash">40083c: b94007e0      ldr     w0, <span class="o">[</span>sp, <span class="c">#0x4]          // Load the result from stack into w0</span>
400840: a9417bfd      ldp     x29, x30, <span class="o">[</span>sp, <span class="c">#0x10]   // Restore Frame Pointer and Link Register</span>
400844: 910083ff      add     sp, sp, <span class="c">#0x20           // Deallocate stack space</span>
400848: d65f03c0      ret                             // Return to <span class="nb">caller</span></code></pre></figure>

<p>The function epilogue restores the caller’s execution environment. 
The return value is loaded from stack memory into register <code class="language-plaintext highlighter-rouge">w0</code>. 
The <code class="language-plaintext highlighter-rouge">ldp</code> instruction performs a pair load to restore the frame pointer (<code class="language-plaintext highlighter-rouge">x29</code>) and link register (<code class="language-plaintext highlighter-rouge">x30</code>). 
Finally, the stack pointer (<code class="language-plaintext highlighter-rouge">sp</code>) is incremented by 32 bytes to perform stack deallocation before the ret instruction 
redirects the Control Flow back to the address stored in the link register.</p>

<h2 id="optimized-analysis">Optimized Analysis</h2>

<p>The following analysis examines the optimized assembly code generated with the <code class="language-plaintext highlighter-rouge">-O2</code> flag.</p>

<figure class="highlight"><pre><code class="language-bash" data-lang="bash"><span class="nv">$ </span><span class="nb">rm</span> <span class="nt">-f</span> <span class="o">(</span>path filter <span class="k">*</span>.out<span class="o">)</span><span class="p">;</span> clang <span class="nt">-O2</span> <span class="nt">-target</span> aarch64-linux-gnu <span class="nt">--sysroot</span><span class="o">=</span>/usr/aarch64-linux-gnu <span class="nt">-static</span> add.c <span class="nt">-o</span> app.out<span class="p">;</span> qemu-aarch64 ./app.out
11 <span class="o">=</span> 11</code></pre></figure>

<figure class="highlight"><pre><code class="language-bash" data-lang="bash"><span class="nv">$ </span>llvm-objdump <span class="nt">-d</span> <span class="nt">--disassemble-symbols</span><span class="o">=</span>add app.out

app.out:        file format elf64-littleaarch64

Disassembly of section .text:

00000000004007f0 &lt;add&gt;:
  4007f0: 0b000020      add     w0, w1, w0
  4007f4: d65f03c0      ret</code></pre></figure>

<h4 id="recursion-call">Recursion Call</h4>

<p>In the unoptimized execution, each recursive call of <code class="language-plaintext highlighter-rouge">add(x + 1, y - 1)</code> triggers the function prologue, 
resulting in the allocation of a new activation record. 
This manifests as <code class="language-plaintext highlighter-rouge">O(n)</code> space complexity, as the stack grows linearly with the input value of <code class="language-plaintext highlighter-rouge">y</code>.</p>

<figure class="highlight"><pre><code class="language-bash" data-lang="bash">|   Higher Address   |
+--------------------+
|  add<span class="o">(</span>1, 10<span class="o">)</span> Frame  | <span class="o">(</span>Initial Call: a <span class="o">=</span> 1, b <span class="o">=</span> 10<span class="o">)</span>
+--------------------+
|  add<span class="o">(</span>2, 9<span class="o">)</span>  Frame  |
+--------------------+
|  add<span class="o">(</span>3, 8<span class="o">)</span>  Frame  |
+--------------------+
|  add<span class="o">(</span>4, 7<span class="o">)</span>  Frame  |
+--------------------+
|  add<span class="o">(</span>5, 6<span class="o">)</span>  Frame  |
+--------------------+
|  add<span class="o">(</span>6, 5<span class="o">)</span>  Frame  |
+--------------------+
|  add<span class="o">(</span>7, 4<span class="o">)</span>  Frame  |
+--------------------+
|  add<span class="o">(</span>8, 3<span class="o">)</span>  Frame  |
+--------------------+
|  add<span class="o">(</span>9, 2<span class="o">)</span>  Frame  |
+--------------------+
|  add<span class="o">(</span>10, 1<span class="o">)</span> Frame  |
+--------------------+
|  add<span class="o">(</span>11, 0<span class="o">)</span> Frame  | <span class="o">(</span>Base Case: x <span class="o">=</span> 11, returns x<span class="o">)</span>
+--------------------+ &lt;<span class="nt">---</span> SP <span class="o">(</span>Current Stack Pointer<span class="o">)</span>
|   Lower Address    |</code></pre></figure>

<h4 id="tail-recursion-call">Tail Recursion Call</h4>

<p>The <code class="language-plaintext highlighter-rouge">-O2</code> optimization level identifies the recursive call as a Tail Call. 
The compiler recognizes that the current stack frame can be reused, 
as no further operations are required after the callee returns. 
This reduces the space complexity from <code class="language-plaintext highlighter-rouge">O(n)</code> to <code class="language-plaintext highlighter-rouge">O(1)</code> and eliminates the overhead associated 
with stack frame allocation and deallocation.</p>

<figure class="highlight"><pre><code class="language-bash" data-lang="bash">|   Higher Address   |    |   Higher Address  |           |   Higher Address  |
+--------------------+    +-------------------+           +-------------------+ &lt;<span class="nt">---</span> Previous SP
|  add<span class="o">(</span>1, 10<span class="o">)</span> Frame  | -&gt; |  add<span class="o">(</span>2, 9<span class="o">)</span> Frame  | -&gt; ... -&gt; |  add<span class="o">(</span>11, 0<span class="o">)</span> Frame |      <span class="o">(</span>Reused <span class="k">for </span>all steps<span class="o">)</span>
+--------------------+    +-------------------+           +-------------------+ &lt;<span class="nt">---</span> SP <span class="o">(</span>Static: Never moves<span class="o">)</span>
|   Lower Address    |    |   Lower Address   |           |   Lower Address   | </code></pre></figure>

<h4 id="arithmetic-folding">Arithmetic Folding</h4>
<p>While Tail Call Optimization handles the stack efficiency, Arithmetic Folding is an optimization technique 
where the compiler analyzes the symbolic behavior of a loop or recursion to replace it with a simplified mathematical expression.</p>

<h4 id="conclusion">Conclusion</h4>
<p>Through Tail Call Optimization and Arithmetic Folding, 
the compiler optimizes the recursive logic by eliminating the stack-related and control-flow instructions (totaling 21 instructions) into a single <code class="language-plaintext highlighter-rouge">add</code> instruction. 
It effectively reduces the space complexity from <code class="language-plaintext highlighter-rouge">O(n)</code> to <code class="language-plaintext highlighter-rouge">O(1)</code> and minimizes the execution latency to a single instruction cycle.</p>]]></content><author><name></name></author><category term="compiler" /><summary type="html"><![CDATA[Study Notes: You can’t fool the optimiser]]></summary></entry><entry><title type="html">Study Notes: Why xor eax, eax?</title><link href="/2026/01/01/Advent-of-Compiler-Optimisations-Study-Notes-01.html" rel="alternate" type="text/html" title="Study Notes: Why xor eax, eax?" /><published>2026-01-01T00:00:00+00:00</published><updated>2026-01-01T00:00:00+00:00</updated><id>/2026/01/01/Advent-of-Compiler-Optimisations-Study-Notes-01</id><content type="html" xml:base="/2026/01/01/Advent-of-Compiler-Optimisations-Study-Notes-01.html"><![CDATA[<h2 id="study-notes-why-xor-eax-eax">Study Notes: Why xor eax, eax?</h2>

<p>These notes are based on the post <a href="https://xania.org/202512/01-xor-eax-eax"><strong>Why xor eax, eax?</strong></a>, which is Day 1 of the <a href="https://xania.org/AoCO2025-archive">Advent of Compiler Optimisations 2025</a> Series by <a href="https://xania.org/MattGodbolt">Matt Godbolt</a>.</p>

<h4 id="development-environment">Development Environment</h4>

<figure class="highlight"><pre><code class="language-bash" data-lang="bash"><span class="nv">$ </span>lsb_release <span class="nt">-d</span>
Description:	Ubuntu 24.04.3 LTS

<span class="nv">$ </span>gcc <span class="nt">-v</span>
gcc version 13.3.0

<span class="nv">$ </span>clang <span class="nt">-v</span>
Ubuntu clang version 18.1.8

<span class="nv">$ </span>llvm-objdump <span class="nt">-v</span>
Ubuntu LLVM version 18.1.8

<span class="nv">$ </span>lldb <span class="nt">-v</span>
lldb version 18.1.8</code></pre></figure>

<h4 id="the-difference-between--o0--o1-and--o2">The difference between <code class="language-plaintext highlighter-rouge">-O0</code>, <code class="language-plaintext highlighter-rouge">-O1</code>, and <code class="language-plaintext highlighter-rouge">-O2</code></h4>

<p>Basically, we know the compiler has the following stages. If we use the same code but choose different 
optimization levels, the final assembly code will be different.</p>

<figure class="highlight"><pre><code class="language-bash" data-lang="bash">                        <span class="o">[</span> FRONTEND <span class="o">]</span>         <span class="o">[</span> MIDDLE-END <span class="o">]</span>          <span class="o">[</span> BACKEND <span class="o">]</span>
                        .----------.         .------------.         .------------.
                        |  Lexer   |         |            |         |    Code    |
Source Code <span class="o">(</span><span class="k">*</span>.c<span class="o">)</span> <span class="nt">---</span><span class="o">&gt;</span>  |    &amp;     |  <span class="nt">---</span><span class="o">&gt;</span>   |  Optimizer |  <span class="nt">---</span><span class="o">&gt;</span>   |  Generator | <span class="nt">---</span><span class="o">&gt;</span> <span class="o">[</span> Assembly <span class="o">(</span><span class="k">*</span>.s<span class="o">)</span> <span class="o">]</span>
                        |  Parser  |         |            |         | <span class="o">(</span>e.g. x86<span class="o">)</span> |
                        <span class="s1">'----------'</span>         <span class="s1">'------------'</span>         <span class="s1">'------------'</span>       </code></pre></figure>

<p>For now, we use <code class="language-plaintext highlighter-rouge">main.c</code> as input (shown below), apply different optimization levels, 
and use <code class="language-plaintext highlighter-rouge">llvm-objdump</code> to analyze the corresponding assembly code.</p>

<figure class="highlight"><pre><code class="language-bash" data-lang="bash"><span class="nv">$ </span>nvim main.c</code></pre></figure>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">int</span> <span class="nf">main</span><span class="p">()</span> <span class="p">{</span>
  <span class="k">return</span> <span class="mi">0</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div>

<h6 id="use--o0-as-optimization-level">Use <code class="language-plaintext highlighter-rouge">-O0</code> as optimization level</h6>

<figure class="highlight"><pre><code class="language-bash" data-lang="bash"><span class="nv">$ </span><span class="nb">rm</span> <span class="nt">-f</span> <span class="k">*</span>.o<span class="p">;</span> gcc <span class="nt">-O0</span> <span class="nt">-c</span> main.c<span class="p">;</span> llvm-objdump <span class="nt">-d</span> <span class="nt">--x86-asm-syntax</span><span class="o">=</span>att main.o</code></pre></figure>

<figure class="highlight"><pre><code class="language-bash" data-lang="bash">main.o: file format elf64-x86-64

Disassembly of section .text:

0000000000000000 &lt;main&gt;:
       0: f3 0f 1e fa                   endbr64
       4: 55                            pushq %rbp
       5: 48 89 e5                      movq  %rsp, %rbp
       8: b8 00 00 00 00                movl  <span class="nv">$0x0</span>, %eax
       d: 5d                            popq  %rbp
       e: c3                            retq</code></pre></figure>

<figure class="highlight"><pre><code class="language-bash" data-lang="bash"><span class="nv">$ </span>size main.o</code></pre></figure>

<figure class="highlight"><pre><code class="language-bash" data-lang="bash">text	   data	    bss	    dec	    hex	filename
 103	      0	      0	    103	     67	main.o</code></pre></figure>

<p>As <code class="language-plaintext highlighter-rouge">-O0</code>, the compiler generates a stack frame, leading to unnecessary instruction overhead.</p>

<h6 id="use--o1-as-optimization-level">Use <code class="language-plaintext highlighter-rouge">-O1</code> as optimization level</h6>

<figure class="highlight"><pre><code class="language-bash" data-lang="bash"><span class="nv">$ </span><span class="nb">rm</span> <span class="nt">-f</span> <span class="k">*</span>.o<span class="p">;</span> gcc <span class="nt">-O1</span> <span class="nt">-c</span> main.c<span class="p">;</span> llvm-objdump <span class="nt">-d</span> <span class="nt">--x86-asm-syntax</span><span class="o">=</span>att main.o</code></pre></figure>

<figure class="highlight"><pre><code class="language-bash" data-lang="bash">main.o: file format elf64-x86-64

Disassembly of section .text:

0000000000000000 &lt;main&gt;:
       0: f3 0f 1e fa                   endbr64
       4: b8 00 00 00 00                movl  <span class="nv">$0x0</span>, %eax
       9: c3                            retq</code></pre></figure>

<figure class="highlight"><pre><code class="language-bash" data-lang="bash"><span class="nv">$ </span>size main.o</code></pre></figure>

<figure class="highlight"><pre><code class="language-bash" data-lang="bash">text	   data	    bss	    dec	    hex	filename
  90	      0	      0	     90	     5a	main.o</code></pre></figure>

<p>It reduces the output from six instructions to three by removing the stack frame setup.</p>

<h6 id="use--o2-as-optimization-level">Use <code class="language-plaintext highlighter-rouge">-O2</code> as optimization level</h6>

<figure class="highlight"><pre><code class="language-bash" data-lang="bash"><span class="nv">$ </span><span class="nb">rm</span> <span class="nt">-f</span> <span class="k">*</span>.o<span class="p">;</span> gcc <span class="nt">-O2</span> <span class="nt">-c</span> main.c<span class="p">;</span> llvm-objdump <span class="nt">-d</span> <span class="nt">--x86-asm-syntax</span><span class="o">=</span>att main.o</code></pre></figure>

<figure class="highlight"><pre><code class="language-bash" data-lang="bash">main.o: file format elf64-x86-64

Disassembly of section .text:

0000000000000000 &lt;main&gt;:
       0: f3 0f 1e fa                   endbr64
       4: 31 c0                         xorl  %eax, %eax
       6: c3                            retq</code></pre></figure>

<figure class="highlight"><pre><code class="language-bash" data-lang="bash"><span class="nv">$ </span>size main.o</code></pre></figure>

<figure class="highlight"><pre><code class="language-bash" data-lang="bash">text	   data	    bss	    dec	    hex	filename
  87	      0	      0	     87	     57	main.o</code></pre></figure>

<p>As you can see, <code class="language-plaintext highlighter-rouge">-O2</code> and <code class="language-plaintext highlighter-rouge">-O1</code> both produce three instructions. 
The only differences is that <code class="language-plaintext highlighter-rouge">-O2</code> changes from <code class="language-plaintext highlighter-rouge">movl</code> to <code class="language-plaintext highlighter-rouge">xorl</code>. 
The reason is the instruction size. <code class="language-plaintext highlighter-rouge">xorl %eax, %eax</code> only use two bytes,
making it smaller than the five bytes <code class="language-plaintext highlighter-rouge">movl  $0x0, %eax</code>.
Hence, you can see the total <code class="language-plaintext highlighter-rouge">.text</code> size reduces from 90 bytes to 87 bytes.</p>

<h4 id="how-about-we-change-gcc-to-clang">How about we change <code class="language-plaintext highlighter-rouge">gcc</code> to <code class="language-plaintext highlighter-rouge">clang</code>?</h4>

<figure class="highlight"><pre><code class="language-bash" data-lang="bash"><span class="nv">$ </span><span class="nb">rm</span> <span class="nt">-f</span> <span class="k">*</span>.o<span class="p">;</span> clang <span class="nt">-O1</span> <span class="nt">-c</span> main.c<span class="p">;</span> llvm-objdump <span class="nt">-d</span> <span class="nt">--x86-asm-syntax</span><span class="o">=</span>att main.o</code></pre></figure>

<figure class="highlight"><pre><code class="language-bash" data-lang="bash"><span class="nb">rm</span> <span class="nt">-f</span> <span class="k">*</span>.o<span class="p">;</span> clang <span class="nt">-O1</span> <span class="nt">-c</span> main.c<span class="p">;</span> llvm-objdump <span class="nt">-d</span> <span class="nt">--x86-asm-syntax</span><span class="o">=</span>att main.o

main.o: file format elf64-x86-64

Disassembly of section .text:

0000000000000000 &lt;main&gt;:
       0: 31 c0                         xorl  %eax, %eax
       2: c3                            retq</code></pre></figure>

<p>You will find that the Clang’s <code class="language-plaintext highlighter-rouge">-O1</code> output already uses <code class="language-plaintext highlighter-rouge">xorl</code>, making it similar to GCC’s <code class="language-plaintext highlighter-rouge">-O2</code>.
Additionally, it consists of only two instructions because Clang does not generate the <code class="language-plaintext highlighter-rouge">endbr64</code> instruction.</p>

<h4 id="why-eax-not-rax-">Why <code class="language-plaintext highlighter-rouge">eax</code>, not <code class="language-plaintext highlighter-rouge">rax</code> ?</h4>

<figure class="highlight"><pre><code class="language-bash" data-lang="bash"><span class="nv">$ </span>nvim get_val.c</code></pre></figure>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">long</span> <span class="nf">get_val</span><span class="p">()</span> <span class="p">{</span>
  <span class="k">return</span> <span class="mi">0</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div>

<figure class="highlight"><pre><code class="language-bash" data-lang="bash"><span class="nv">$ </span><span class="nb">rm</span> <span class="nt">-f</span> <span class="k">*</span>.o<span class="p">;</span> clang <span class="nt">-O2</span> <span class="nt">-c</span> get_val.c<span class="p">;</span> llvm-objdump <span class="nt">-d</span> <span class="nt">--x86-asm-syntax</span><span class="o">=</span>att get_val.o</code></pre></figure>

<figure class="highlight"><pre><code class="language-bash" data-lang="bash">get_val.o:  file format elf64-x86-64

Disassembly of section .text:

0000000000000000 &lt;get_val&gt;:
       0: 31 c0                         xorl  %eax, %eax
       2: c3                            retq</code></pre></figure>

<p>As we know, the x86-64 calling convention requires the return value to be stored in the 
64-bit <code class="language-plaintext highlighter-rouge">rax</code> register. However, we see that the compiler uses the 32-bit <code class="language-plaintext highlighter-rouge">eax</code> register for the <code class="language-plaintext highlighter-rouge">xorl</code> instruction. 
The reason is that in x86-64, any operation that writes to a 32-bit register automatically zero-extends the result
into the upper 32 bits of the corresponding 64-bit register.</p>

<p>Let’s see an example for verifying Zero-Extension with LLDB</p>

<figure class="highlight"><pre><code class="language-bash" data-lang="bash"><span class="nv">$ </span>nvim main.c</code></pre></figure>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">int</span> <span class="nf">main</span><span class="p">()</span> <span class="p">{</span>
  <span class="k">return</span> <span class="mi">0</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div>

<figure class="highlight"><pre><code class="language-bash" data-lang="bash"><span class="nv">$ </span><span class="nb">rm</span> <span class="nt">-f</span> <span class="k">*</span>.out<span class="p">;</span> clang <span class="nt">-g</span> <span class="nt">-O2</span> <span class="nt">-o</span> app.out main.c<span class="p">;</span> lldb app.out</code></pre></figure>

<figure class="highlight"><pre><code class="language-bash" data-lang="bash"><span class="o">(</span>lldb<span class="o">)</span> target create <span class="s2">"app.out"</span>
Current executable <span class="nb">set </span>to <span class="s1">'/home/gapry/Workspaces/test/app.out'</span> <span class="o">(</span>x86_64<span class="o">)</span><span class="nb">.</span>
<span class="o">(</span>lldb<span class="o">)</span> breakpoint <span class="nb">set</span> <span class="nt">-n</span> main
Breakpoint 1: where <span class="o">=</span> app.out<span class="sb">`</span>main at main.c:2:3, address <span class="o">=</span> 0x0000000000001130
<span class="o">(</span>lldb<span class="o">)</span> r
Process 409020 launched: <span class="s1">'/home/gapry/Workspaces/test/app.out'</span> <span class="o">(</span>x86_64<span class="o">)</span>
Process 409020 stopped
<span class="k">*</span> thread <span class="c">#1, name = 'app.out', stop reason = breakpoint 1.1</span>
    frame <span class="c">#0: 0x0000555555555130 app.out`main at main.c:2:3</span>
   1   	int main<span class="o">()</span> <span class="o">{</span>
-&gt; 2   	  <span class="k">return </span>0<span class="p">;</span>
   3   	<span class="o">}</span>
<span class="o">(</span>lldb<span class="o">)</span> register write rax 0xffffffffffffffff
<span class="o">(</span>lldb<span class="o">)</span> register <span class="nb">read </span>rax
     rax <span class="o">=</span> 0xffffffffffffffff
<span class="o">(</span>lldb<span class="o">)</span> disassemble <span class="nt">--pc</span>
app.out<span class="sb">`</span>main:
-&gt;  0x555555555130 &lt;+0&gt;: xorl   %eax, %eax
    0x555555555132 &lt;+2&gt;: retq
    0x555555555133:      addb   %dh, %bl
<span class="o">(</span>lldb<span class="o">)</span> thread step-inst
Process 409020 stopped
<span class="k">*</span> thread <span class="c">#1, name = 'app.out', stop reason = instruction step into</span>
    frame <span class="c">#0: 0x0000555555555132 app.out`main at main.c:2:3</span>
   1   	int main<span class="o">()</span> <span class="o">{</span>
-&gt; 2   	  <span class="k">return </span>0<span class="p">;</span>
   3   	<span class="o">}</span>
<span class="o">(</span>lldb<span class="o">)</span> disassemble <span class="nt">--pc</span>
app.out<span class="sb">`</span>main:
-&gt;  0x555555555132 &lt;+2&gt;: retq
    0x555555555133:      addb   %dh, %bl
<span class="o">(</span>lldb<span class="o">)</span> register <span class="nb">read </span>rax
     rax <span class="o">=</span> 0x0000000000000000
<span class="o">(</span>lldb<span class="o">)</span></code></pre></figure>

<p>The LLDB output confirms that even though <code class="language-plaintext highlighter-rouge">xorl</code> only targets the lower 32 bits <code class="language-plaintext highlighter-rouge">%eax</code> register,
the hardware automatically cleared the entire 64-bit <code class="language-plaintext highlighter-rouge">%rax</code> register.</p>

<h4 id="callercallee-arguments">Caller/Callee Arguments</h4>

<figure class="highlight"><pre><code class="language-bash" data-lang="bash"><span class="nv">$ </span>nvim main.c</code></pre></figure>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">extern</span> <span class="kt">void</span> <span class="nf">g</span><span class="p">(</span><span class="kt">long</span> <span class="n">arg1</span><span class="p">,</span> 
              <span class="kt">long</span> <span class="n">arg2</span><span class="p">,</span> 
              <span class="kt">long</span> <span class="n">arg3</span><span class="p">,</span> 
              <span class="kt">long</span> <span class="n">arg4</span><span class="p">,</span> 
              <span class="kt">long</span> <span class="n">arg5</span><span class="p">,</span> 
              <span class="kt">long</span> <span class="n">arg6</span><span class="p">);</span>

<span class="kt">void</span> <span class="nf">f</span><span class="p">()</span> <span class="p">{</span>
  <span class="n">g</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="mi">0</span><span class="p">,</span> <span class="mi">0</span><span class="p">,</span> <span class="mi">0</span><span class="p">,</span> <span class="mi">0</span><span class="p">,</span> <span class="mi">0</span><span class="p">);</span>
<span class="p">}</span>
</code></pre></div></div>

<figure class="highlight"><pre><code class="language-bash" data-lang="bash"><span class="nv">$ </span><span class="nb">rm</span> <span class="nt">-f</span> <span class="k">*</span>.o<span class="p">;</span> clang <span class="nt">-O2</span> <span class="nt">-c</span> main.c<span class="p">;</span> llvm-objdump <span class="nt">-d</span> <span class="nt">--disassemble-symbols</span><span class="o">=</span>f <span class="nt">--x86-asm-syntax</span><span class="o">=</span>att main.o</code></pre></figure>

<figure class="highlight"><pre><code class="language-bash" data-lang="bash">main.o:	file format elf64-x86-64

Disassembly of section .text:

0000000000000000 &lt;f&gt;:
       0: 31 ff                        	xorl	%edi, %edi
       2: 31 f6                        	xorl	%esi, %esi
       4: 31 d2                        	xorl	%edx, %edx
       6: 31 c9                        	xorl	%ecx, %ecx
       8: 45 31 c0                     	xorl	%r8d, %r8d
       b: 45 31 c9                     	xorl	%r9d, %r9d
       e: e9 00 00 00 00               	jmp	0x13 &lt;f+0x13&gt;</code></pre></figure>

<p>According to the x86-64 System V ABI, the first six integer or pointer arguments are passed in
specific registers. To pass <code class="language-plaintext highlighter-rouge">0</code> to all of them, the compiler again uses the <code class="language-plaintext highlighter-rouge">xorl</code> optimization to
zero out each one:</p>

<table>
  <thead>
    <tr>
      <th style="text-align: left">Argument</th>
      <th style="text-align: left">64-bit Register</th>
      <th style="text-align: left">32-bit Register</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="text-align: left">1st</td>
      <td style="text-align: left"><code class="language-plaintext highlighter-rouge">%rdi</code></td>
      <td style="text-align: left"><code class="language-plaintext highlighter-rouge">%edi</code></td>
    </tr>
    <tr>
      <td style="text-align: left">2nd</td>
      <td style="text-align: left"><code class="language-plaintext highlighter-rouge">%rsi</code></td>
      <td style="text-align: left"><code class="language-plaintext highlighter-rouge">%esi</code></td>
    </tr>
    <tr>
      <td style="text-align: left">3rd</td>
      <td style="text-align: left"><code class="language-plaintext highlighter-rouge">%rdx</code></td>
      <td style="text-align: left"><code class="language-plaintext highlighter-rouge">%edx</code></td>
    </tr>
    <tr>
      <td style="text-align: left">4th</td>
      <td style="text-align: left"><code class="language-plaintext highlighter-rouge">%rcx</code></td>
      <td style="text-align: left"><code class="language-plaintext highlighter-rouge">%ecx</code></td>
    </tr>
    <tr>
      <td style="text-align: left">5th</td>
      <td style="text-align: left"><code class="language-plaintext highlighter-rouge">%r8</code></td>
      <td style="text-align: left"><code class="language-plaintext highlighter-rouge">%r8d</code></td>
    </tr>
    <tr>
      <td style="text-align: left">6th</td>
      <td style="text-align: left"><code class="language-plaintext highlighter-rouge">%r9</code></td>
      <td style="text-align: left"><code class="language-plaintext highlighter-rouge">%r9d</code></td>
    </tr>
  </tbody>
</table>

<p>The <code class="language-plaintext highlighter-rouge">xorl</code> optimization does not only appear for return values, you will also see it frequently
when a caller prepares arguments for a callee. As with the previous example, zeroing the 32-bit
version of these registers automatically zero-extends to the full 64-bit register.</p>

<h2 id="references">References</h2>
<ul>
  <li><a href="https://learn.microsoft.com/en-us/windows-hardware/drivers/debugger/x64-architecture">x64 architecture</a></li>
</ul>]]></content><author><name></name></author><category term="compiler" /><summary type="html"><![CDATA[Study Notes: Why xor eax, eax?]]></summary></entry></feed>